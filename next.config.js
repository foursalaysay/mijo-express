// next.config.js
/**
 * @type {import('next').NextConfig}
 */
const nextConfig = {
    images: {
      domains: ['via.placeholder.com'], // Add the domains you're fetching images from
    },
  };
  
  module.exports = nextConfig;
  